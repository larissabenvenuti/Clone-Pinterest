import { StyleSheet } from 'react-native';

export const inputStyles = StyleSheet.create({
  inputText: {
    marginTop: 15,
    borderColor: '#333333',
    borderWidth: 1,
    borderRadius: 15,
    paddingHorizontal: 15,
    height: 50,
    width: '100%',
    marginBottom: 30,
    fontSize: 16,
  },
});
